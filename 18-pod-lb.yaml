apiVersion: v1
kind: Service
metadata:
  name: service-lb-demo
  labels:
    project: roboshop
    component: frontend
    environment: dev
    purpose: Load-balancer
spec:
  containers:
  - name: nginx
    image: nginx